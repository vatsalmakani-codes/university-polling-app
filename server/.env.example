# MongoDB Connection String (replace with your own from local MongoDB or Atlas)
MONGO_URI=mongodb+srv://<username>:<password>@your_cluster_url/university_polling

# JSON Web Token Secret Key (use a long, random string)
JWT_SECRET=your_super_strong_jwt_secret_key

# Port for the backend server
PORT=5000